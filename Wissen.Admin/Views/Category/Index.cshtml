@model IEnumerable<Wissen.Model.Category>
@{
<<<<<<< HEAD
    ViewBag.Title = "Kategori";
}

<h2>Test</h2>
<a href="@Url.Action("Create")" class="btn btn-primary">Yeni Kategori</a>
<!-- -->
=======
    ViewBag.Title = "Kategoriler";
}
<h2>Kategoriler</h2>
<a href="@Url.Action("Create")" class="btn btn-primary">Yeni Kategori</a>
>>>>>>> ea864b23c481a57cccfc58da7afa2b708e1a67f6
<table class="table table-hover table-striped">
    <tr>
        <th>Ad</th>
        <th>Açıklama</th>
<<<<<<< HEAD
        <th>Tarih</th>       
    </tr>
    @foreach (var item in Model)
    {
    <tr>
        <td><a href="@Url.Action("Edit",new { id = item.Id})">@item.Name</a></td>
        <td>@item.Description</td>
        <td>@item.CreatedAt.ToString("dd.MM.yyyy")</td>
        <td><a href="@Url.Action("Edit",new { id= item.Id})" class="btn btn-primary" btn-sm>Düzenle</a>
        <a href="@Url.Action("Delete",new { id= item.Id})" class="btn btn-danger" btn-sm>Sil</a></td>
    </tr>
    }
</table>


=======
        <th>Tarih</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td><a href="@Url.Action("Edit", new { id = item.Id })">@item.Name</a></td>
            <td>@item.Description</td>
            <td>@item.CreatedAt.ToString("dd.MM.yyyy")</td>
            <td><a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-primary btn-sm">Düzenle</a>
            <a href="@Url.Action("Delete", new { id = item.Id })" class="btn btn-danger btn-sm">Sil</a></td>
        </tr>
    }
</table>
>>>>>>> ea864b23c481a57cccfc58da7afa2b708e1a67f6
